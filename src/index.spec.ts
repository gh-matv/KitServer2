
import { expect } from "chai";
import "mocha";
import User from "./core/models/user";

describe("This", () => {
	describe("should", () => {

		it("always pass", () => {
			expect(true).equal(true);
		});
	});
});
